@page
@model asp_geosystems_store.Pages.IndexModel
@{
    ViewData["Title"] = "Home page";
}
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

    <div class="text-center" id="top">
    <h1>Welcome to Geosystems</h1>
    <h2>All geospatial solutions here</h2>
    <hr style="color: white;"/>
    @if (SignInManager.IsSignedIn(User))
    {
    <button class="btn btn-sm order-button animate opa animate--delay animate--slow  Main-Button"><a asp-page="/Menu">Order Now</a></button>
    }
</div>
